import { formatVariables } from 'particle/helpers/format-variables';
import { module, test } from 'qunit';

module('Unit | Helper | format variables');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = formatVariables([42]);
  assert.ok(result);
});
