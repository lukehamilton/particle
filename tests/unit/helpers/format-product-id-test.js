import { formatProductId } from 'particle/helpers/format-product-id';
import { module, test } from 'qunit';

module('Unit | Helper | format product id');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = formatProductId([42]);
  assert.ok(result);
});
